# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ZapCom' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ZapCom
  pod 'SDWebImage', '~> 5.0'
  target 'ZapComTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ZapComUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
